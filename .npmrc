# for pnpm
shamefully-hoist=true
auto-install-peers=true
# pnpm v10+ blocks postinstall scripts by default and errors out.
# Slidev/Vite bundle their own esbuild so we don't need the native-binary postinstall.
ignore-scripts=true
